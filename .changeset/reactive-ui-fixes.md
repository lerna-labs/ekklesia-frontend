---
"ekklesia-frontend": patch
---

Fix several parts of the UI that did not refresh when their underlying data changed. Voter badge labels, the vote participation donut and its percentages, the sort controls, and voter profile pages now update in place as the data changes instead of keeping their first-loaded values.
